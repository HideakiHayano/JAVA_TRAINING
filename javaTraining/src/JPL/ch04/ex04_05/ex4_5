(a)具象クラス

(b)具象クラス
探索の実行方法まで具体的に決まっているため。

(c)インターフェース
このインターフェースではオブジェクトを書くためのメソッドの宣言のみが行われている。、
このインターフェースを実装することでオブジェクトの描画方法を
具体的に定義するという使い方ができる。

(d)抽象クラス
グラフィックデスクトップから実行するという性質から、定数以外のフィールドを
持つ可能性があり、メソッドの実行方法はまだ具体的に決めることができない。
